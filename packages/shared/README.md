# Shared (placeholder)

Paquete compartido para utilidades y tipos entre `backend` y `frontend`.

Comandos:

```powershell
pnpm --filter @sistema/shared install
pnpm --filter @sistema/shared run build
```
