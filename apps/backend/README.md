# Backend (placeholder)

Scaffold mínimo para el backend (NestJS) — pendiente de inicializar.

Comandos:

```powershell
pnpm --filter @sistema/backend install
pnpm --filter @sistema/backend run dev
```
