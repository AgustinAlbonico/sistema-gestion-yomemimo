# Frontend (placeholder)

Scaffold mínimo para el frontend (Vite + React + TypeScript) — pendiente de inicializar.

Comandos:

```powershell
pnpm --filter @sistema/frontend install
pnpm --filter @sistema/frontend run dev
```
